name: Deploy bizmap to GitHub Pages

on:
  push:
    branches:
      - claude/hopeful-johnson-fr5zin
      - main
    paths:
      - "bizmap/**"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./bizmap
          publish_branch: gh-pages
